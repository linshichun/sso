<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="win.scolia.sso.dao.PermissionMapper">

    <insert id="insertPermission">
        INSERT INTO permission (permission, create_time, last_modified)
        VALUES (#{permission}, #{createTime}, #{lastModified})
    </insert>

    <insert id="insertRolePermission">
        INSERT INTO role_permission (role_id, permission_id, create_time, last_modified)
        VALUES (#{roleId}, #{permissionId}, #{createTime}, #{lastModified})
    </insert>

    <delete id="deletePermission">
        DELETE FROM permission
        WHERE permission = #{permission}
    </delete>

    <delete id="deleteRolePermissionMapByPermissionId">
        DELETE FROM role_permission
        WHERE permission_id = #{permissionId}
    </delete>

    <delete id="deleteRolePermissionMapByRoleId">
        DELETE FROM role_permission
        WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteRolePermissionMapByRoleIdAndPermissionId">
        DELETE FROM role_permission
        WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </delete>

    <update id="updatePermission">
        UPDATE permission
        SET permission = #{permission.permission}, last_modified = #{permission.lastModified}
        WHERE permission = #{oldPermission}
    </update>

    <select id="selectPermissionsByRoleName" resultType="string">
        SELECT permission
        FROM permission
            JOIN role_permission
                ON permission.permission_id = role_permission.permission_id
            JOIN role
                ON role_permission.role_id = role.role_id
        WHERE role.role_name = #{roleName}
    </select>

    <resultMap id="permissionMap" type="win.scolia.sso.bean.entity.Permission">
        <id column="permission_id" property="permissionId"/>
        <result column="permission" property="permission"/>
        <result column="create_time" property="createTime"/>
        <result column="last_modified" property="lastModified"/>
    </resultMap>

    <select id="selectPermission" resultMap="permissionMap">
        SELECT
            permission_id,
            permission,
            create_time,
            last_modified
        FROM permission
        WHERE permission = #{permission}
    </select>

    <select id="selectAllPermissions" resultMap="permissionMap">
        SELECT
            permission_id,
            permission,
            create_time,
            last_modified
        FROM permission
    </select>
</mapper>